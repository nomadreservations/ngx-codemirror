{
  "buildCommand": "npm run build:release",
  "afterReleaseCommand": "npm run docs:publish && echo Successfully released ${version} to ${dist.repo}.",
  "npm": {
    "publishPath": "./dist/ngx-codemirror"
  }
}
